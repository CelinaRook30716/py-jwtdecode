# JWT payload decoder (no verify)

```
jwtdecode.py
```
See the test next to the source for usage.

Decode a JWT payload for inspection — base64 only, no signature verification.

No dependencies, no service to run — just the standard library.
